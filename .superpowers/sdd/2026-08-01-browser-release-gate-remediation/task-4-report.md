# Task 4 Report: Authenticated accessibility blockers

## Status

Complete. Populated Journey, dashboard, log history, and authenticated 404 states pass WCAG A/AA axe checks on desktop and mobile. The mobile schedule and log tables expose named, keyboard-focusable scroll regions with visible focus indicators.

## Files

- `tests/accessibility/accessibility.spec.js`
- `templates/pages/journey/_schedule.html`
- `templates/dashboard/dashboard.html`
- `templates/logging/view_logs.html`
- `templates/errors/404.html`
- `static/css/tailwind.css`
- `static/css/style.css` (generated by `npm run build:css`)
- `.superpowers/sdd/2026-08-01-browser-release-gate-remediation/task-4-report.md`

## Implementation

- Added populated-state accessibility coverage using the project-specific Journey review fixtures and `today-targeted@example.com` for dashboard, log history, and authenticated 404.
- Added the shared `.horizontal-scroll-region` contract with `tabindex="0"`, route-specific accessible names, screen-reader scroll instructions, and a two-pixel semantic focus outline.
- Limited focusability assertions to mobile regions whose computed `scrollWidth` is greater than `clientWidth`.
- Added semantic table captions for Recent Logs and Log history so the tables can be found by their computed accessible names.
- Darkened the light-theme constructive token from `#55755F` to `#4B6B55`; the resulting contrast is 4.67:1 against constructive-soft and 4.62:1 against milestone-soft. The existing dark pair remains 5.22:1.
- Corrected audited dashboard/log/404 legacy gray utility pairs locally and used the ink token for archived Journey table headings on milestone-soft.
- Removed the nested 404 `main` landmark and changed authenticated recovery from Dashboard to Today.

## TDD evidence

### RED

- `npx playwright test tests/accessibility/accessibility.spec.js --project=chromium-desktop`
  - 4 failed, 6 passed.
  - Reproduced `color-contrast` failures in populated Journey, dashboard, log history, and 404.
- `npx playwright test tests/accessibility/accessibility.spec.js --project=chromium-mobile`
  - 4 failed, 6 passed.
  - Reproduced the same contrast failures plus `scrollable-region-focusable` on the Journey schedule and dashboard Recent Logs.
- The first sandboxed desktop attempt could not bind the localhost test server; it was rerun with the required local-server permission to obtain the application-level RED result.

### GREEN

- `npm run build:css` — passed.
- `npx playwright test tests/accessibility/accessibility.spec.js --project=chromium-desktop` — 10 passed.
- `npx playwright test tests/accessibility/accessibility.spec.js --project=chromium-mobile` — 10 passed.
- `npx playwright test tests/browser/journey.spec.js --project=chromium-mobile` — 5 passed.
- `.venv/bin/python -m pytest tests/integration/test_journey.py tests/integration/test_analytics_pages.py tests/api/test_plan_endpoints.py::TestApiErrorEnvelope::test_html_404_still_renders_page tests/smoke/test_environment.py -q` — 26 passed, 2 pre-existing Flask-WTF deprecation warnings.
- `git diff --check` — passed.

## Self-review

- Confirmed all added accessibility assertions exercise rendered browser behavior: axe results, overflow geometry, focusability, accessible names, computed outline styles, landmark count, and the recovery link within the main landmark.
- Confirmed the scroll contract is applied only to populated table regions that demonstrably overflow on the mobile viewport.
- Confirmed no routes, models, fixture data, dependencies, branding, favicon, PWA, analytics, or theme-selection behavior changed.
- Confirmed the generated CSS is synchronized with the Tailwind source.
- Reviewed the scoped diff for accidental unrelated edits and whitespace errors.

## Concerns

None. The focused pytest run continues to emit two existing `flask.Markup` deprecation warnings from Flask-WTF.

## Fix round 1: explicit light/dark axe coverage

### Review finding

The original four populated-state axe cases relied on the browser's resolved system/default theme. They did not persist an explicit appearance choice or prove which effective `data-theme` axe evaluated.

### Changes

- Parameterized populated Journey, dashboard, log history, and authenticated 404 axe cases over explicit `light` and `dark` choices.
- Seeded the app's real `nicotine-tracker-theme` local-storage preference before the first navigation and asserted the exact resolved `html[data-theme]` value before each axe scan.
- Separated mobile overflow geometry/focus and 404 landmark/recovery checks from the theme matrix so theme-independent behavior is not duplicated.
- Added a 404-local `dark:text-indigo-400` treatment for the large 404 numeral and rebuilt generated CSS.

### RED

- `npx playwright test tests/accessibility/accessibility.spec.js --project=chromium-desktop`
  - 1 failed, 14 passed, 2 mobile-only tests skipped.
  - The new explicit-dark 404 case found `#4f39f6` on `#111915` at 2.76:1, below the 3:1 WCAG AA threshold for large text.
  - The other seven explicit light/dark populated-state combinations passed, confirming the failure was narrowly scoped.
- Mutation relevance: removing the 404's dark override recreates the exact `color-contrast` failure above, so the new dark-theme case catches a concrete production regression rather than source text changes.

### GREEN

- `npm run build:css` — passed.
- `npx playwright test tests/accessibility/accessibility.spec.js --project=chromium-desktop` — 15 passed, 2 intentional mobile-only skips.
- `npx playwright test tests/accessibility/accessibility.spec.js --project=chromium-mobile` — 17 passed.
- `npx playwright test tests/browser/journey.spec.js --project=chromium-mobile` — 5 passed.
